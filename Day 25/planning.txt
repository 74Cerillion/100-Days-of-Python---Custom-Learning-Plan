report = list()

#1
//for each server, cpu, service in zip(servers, cpu_usage, services_running):
    //report.append({"server": server,
                     "cpu": cpu,
                     "service": service,})

#2
//for i in enumerate(report):
    //print(i)

#3
//cpu = [False if i['cpu'] <= 85 else True for i in report]
//if any(cpu):
    //print(True)
//if all(cpu):
    //print(False)