Necessary Data:
    Account Name - str - instance
    Account Balance - Int - instance
        Cannot be negative
    Total Currency in bank - class
        Cannot go negative

    Transactions for each account - List - instance


//init class
    //totalCurrency = 0

    //define init with self, Name, Balance
        //self name = name
        
        #validate balance
        //if balance < 0
            //raise ValueError

        //self transactions = ()

        //totalCurrency += balance

    //def deposit_amt(self, amount):
        //while True:
            //if amount <= 0:
                //print("Must depo pos amt")
                //input
                //continue
            //break

        //self.balance += amount

        //transactions append statement of change
            #include amt, depo, and time

    //def withdraw_amt(self, amt):
        //while True:
            //if amt <= 0:
                //print("Must withdraw pos amt")
                //input
                //continue
            //break
        
        //if self.balance - amt < 0:
            //raise ValueError
                //insufficient balance
        //else
            //self.balance -= amt
        
        //transactions append statement of change
            #include amt, withdraw, and time
        
    //def transfer(self, target account, amt)
        //if self.balance - amt < 0
            //raise ValueError
        
        //try
            //self.balance -= amt
            //target.balance += amt
        //except #intentional catchall (no specific error named)
            //bounce transaction, let user know it Failed
        
        //transactions append statement of change
            #include amt, transfer, and time

CREATE USER INTERFACE // CLI WILL DO
***anything more is outside the scope of today's learning endeavors

//import sys and class

//initialize dictionary to store account instances
    //put 4 in there for persistence (TEST CASES)
*Ability to add more later is easy, and outside of today's scope
#See the assignment description for scope and purpose#

//display welcome message

//ask user for username

//while True
    #validate username
    //if username in dict keys
        //break
    //else
        //user does not exist
        //sys exit

//display menu with options correlated to numbers

//while True
    //accept input for user decision
        #validate input
        //if input in menu options
            //break
        //else
            //print invalid message

//if choice == 1
    //prompt for amount
    //call deposit method, pass amt

// if choice == 2
    //prompt for amt
    //call withdrawal method, pass amt

// if choice == 3
    //prompt for amt
    //prompt for account to transfer to

    //if target does not exit
        //#VALIDATE TARGET Account
    //else
        //write way to transform target into its object for passing

    //call transfer, pass target and amt in that order

//thank user for using
//end program