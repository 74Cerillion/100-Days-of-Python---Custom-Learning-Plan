//import pathlib Path #create absolute filepath

//buildpath = Path(__file__).resolve().parent
//actualPath = buildpath / 'logFile.txt'

#user choice for what type of log to look for
//stat = input status to search for

#function that reads one line into memory at a time
//def read file()
    //with open actualPath in read mode as f
        //for line in f
                //yield line

#call generator and if line matches condition,
    #pass it to processing logic
//parse file = read file()
//for line in parse file:
    //next(line)
    //if stat in line:
        //formatted = formatLine(line)
        //print formatted

#line processing logic
//def formatLine(line):
    //(add logic to format the line to desired dictionary)
    //use split() on the '|' pipe to divide the strings,
    //use strip to get rid of whitespace.
    //save these in variables "timestamp", "severity", "hostname", "message"
    //add to dictionary with variable name as dict key



    CONTROL FLOW::::::::::::::::::::::::::::::::::::::::::::::::::::::::
Build path to log file -> Ask user for what they want to search for -> 
open file in read mode -> read file into memory one line at a time -> 
implement generator to pause the function unless invoked -> create generator
object in parseFile -> call each line, line by line, from generator -> 
if line meets user criteria from step 2 -> pass line to formatLine() ->
format line -> print formatted line

functions:
    readFile()
    formatLine()

objects:    
    parseLine